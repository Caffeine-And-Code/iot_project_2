#include "TemperatureAgent.h"
#include "config/Config.h"

void TemperatureAgent::update()
{
    // if (this->controller->temperature->getTemperature() < MAX_TEMP)
    // {
    //     this->initialMillisecOverTemperature = millis();
    // }

    // if (this->initialMillisecOverTemperature + MAX_TEMP_TIME_SECONDS * 100 <= millis())
    // {
    //     this->controller->changeState(MaxTemperature);
    // }
}